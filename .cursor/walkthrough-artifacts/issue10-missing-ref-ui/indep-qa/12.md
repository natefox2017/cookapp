# Independent UI QA — Page 12 Following/Source Filter

Agent: independent (no implementer edits). Figma not modified.  
Instance: `119:12274` (file `FHbikS2jILAeMv8mote0vD`).  
Evidence: one `get_screenshot` `119:12274` `maxDimension` 400 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/12-119-12274.png` (185×400).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Pack: `docs/ui-screenshots/missing-ref/10、12/IMG_4424.PNG` is **page 07 Folders / Create a new folder** — **not** required for this page. Do not demand Folders UI.

Expected (this page): Filter title, All Foods, Following RecipeTin Eats / Nagi.

```
VERDICT: PASS
Screenshot fidelity: PASS — Live `119:12274` is Following/Source Filter, not Folders/Create folder and not PixelBase-only. Green **Filter** title; **All Foods** row with selected check; **Following** section with **RecipeTin Eats** / **Nagi** and selected check; close X + circular confirm; Cookbook tab + search circle. IMG_4424 (Create folder / Folders list) correctly unused as a 1:1 still.
Liquid Glass: PASS — Glass limited to nav/control layer (header X/check, floating tab bar, search circle). All Foods / Following rows are opaque content cards, not glass slabs. Light appearance present on this frame.
Consistency (shared chrome/tokens): PASS — Green Large title, circular header buttons, floating tab + search match shared kit; screen was not restyled into Smart Folder / Create Folder.
Control states: PASS* — Default chrome + selected checks on All Foods and RecipeTin Eats; Cookbook tab active. Hover / disabled / loading not shown on this static frame (shared-kit residual, not a per-screen fork).
Violations: none blocking.
Required redo: none.
```

Non-blocking: pack `10、12/` has no dedicated Following still (4423 Smart Folder filters; 4424–4425 page 07). iOS `CookbookShellViews` still uses route placeholders — this verdict is the Figma instance only. Dark appearance not on this instance.
