# Independent UI QA — 14 Category

Instance `119:12208` (file `FHbikS2jILAeMv8mote0vD`). Live `get_screenshot` maxDimension 400 (original 440×1487). Did **not** edit Figma.

**Owner refs (canonical):** `docs/ui-screenshots/missing-ref/22/IMG_4452.PNG` + `IMG_4453.PNG` — Category sheet, empty radios, **Search Categories**, list **Alcoholic Beverage → Vegetarian**. Ignore status bar / Dynamic Island / keyboard.

**Not a ref:** `docs/ui-screenshots/missing-ref/13-15/IMG_4437` = Manage Fields.

Evidence: live instance is a **tall stitch** (green **Category**, check, full list, Search Categories). It is **not** the owner modal sheet.

VERDICT: FAIL
Screenshot fidelity: FAIL vs `22/IMG_4452` + `IMG_4453`. List copy Alcoholic Beverage → Vegetarian and empty green radios match. Search Categories is present but in-flow after Vegetarian, not a floating capsule over Salad (`4452`) or over the sheet gutter (`4453`). Missing leading **+** circle. Large green title sits on the same header band as the check instead of **below** a `+` / `✓` row. No large-radius sheet, dimmed underlay, or recipe peek behind the sheet (`4452`). Compact centered black **Category** (`4453`) is not this instance. Did not score `IMG_4437`.
Liquid Glass: FAIL. Owner chrome is circular `+` / filled-check and a regular-glass Search Categories pill over content. Live check is a solid green disc; `+` is absent; search is a flat in-list field. No dark appearance. List body correctly opaque (not a glass slab).
Consistency (shared chrome/tokens): FAIL vs owner Category/Cuisine sheet kit (`+`, check, grouped list, floating search). Shared tab bar N/A (sheet, not a tab root).
Control states: FAIL. Radios default-empty only. No hover / active / disabled / loading on `+`, check, radios, or search. No selected radio. Scrolled header (`4453`) not a named extra.
Violations:
1. Add the leading circular **+** (same diameter/stroke as the check) on instance `119:12208` / source `118:10840`.
2. Restack chrome to match `4452`: `+` left and green `✓` right on row one; large green **Category** on row two; rounded grouped list; Search Categories as a floating glass capsule.
3. Reconstruct sheet chrome (large top radius, underlay, optional New Recipe peek from `4452`) — do not ship a flat 1487px list on white.
4. Keep Search Categories floating over the list (`4452`) or the sheet bottom inset (`4453`); do not pin it as the last list row.
5. Optional extra **14b to the right** (do not steal 01–49) for `4453` compact centered black Category + full tail through Vegetarian.
6. Define default / hover / active / disabled / loading once on shared `+`, check, radio, and search — not page-local forks.
7. Light + dark glass pass on `+`, check, and search; do not flood glass with opaque green (tint the check glyph/fill only).
Required redo: Rebuild `118:10840` (then instance `119:12208`) to 1:1 `IMG_4452` as canonical 14; optionally 14b for `IMG_4453`. Hide PixelBase if it still wins. Do **not** restyle from Manage Fields `IMG_4437`. Re-export live vs `4452`+`4453` (ignore island). Independent QA must PASS before this page is complete.
