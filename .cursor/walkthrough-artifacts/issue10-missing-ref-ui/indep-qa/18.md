# Independent UI QA — Page 18 Search Results

Agent: independent (no implementer edits). Figma not modified.  
Instance: `115:18812` (file `FHbikS2jILAeMv8mote0vD`). Source `115:8090`.  
Owner still: `docs/ui-screenshots/missing-ref/16-21/IMG_4444.PNG` (Query `1`, 6× Matches Ingredient grid).  
Evidence: stagger 80s, then **one** `get_screenshot` `115:18812` `maxDimension` 400 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/18-115-18812.png` (185×400; original 440×956).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc` (1:1), `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Checked: list circle + search query `1` + clear X + close X; 2×3 recipe grid; Matches Ingredient badges.

```
VERDICT: FAIL
Screenshot fidelity: FAIL vs owner IMG_4444 — Live `115:18812` is no longer PixelBase-only and does show Query `1` + 2×3 Matches Ingredient grid (Beef Bourguignon ×3, Cheesy Zucchini Muffins, mayo jar, Kale Side Salad) with list / search / clear / close chrome. Blocking misses vs 4444: (1) extra numeric servings pills (6 / 12 / 4) on result cards — owner stills have only the carrot “Matches Ingredient” badge, no servings counts; (2) card 5 title scrim is wrong (large “Cheesy Zucchini” over the mayonnaise photo, with leftover “Mayonnaise”) — owner is “Mayonnaise” only.
Liquid Glass: PASS* — Glass/chrome on nav layer only (list circle, search pill, clear, close). Recipe photos are content cards, not glass slabs. Light frame only; dark not on this instance.
Consistency (shared chrome/tokens): PASS* — Same circular list/close + search pill as other Search shells; recipe cards reuse the shared photo-card kit. Servings pill leftover is a results-state fork vs the owner still, not a new tab bar.
Control states: PASS* — Default list/close; search filled (query `1`) + clear X (active populated). Hover / disabled / loading not on this static still (shared-kit residual).
Violations: (1) Servings count pills visible on Search Results cards; IMG_4444 has Matches Ingredient only. (2) Mayo card title is “Cheesy Zucchini” (+ leftover “Mayonnaise”); owner copy is “Mayonnaise”.
Required redo: On source `115:8090` (instance `115:18812`): hide/remove servings pills on this results state; set card 5 title to “Mayonnaise” only and drop the stray zucchini overlay. Keep query `1`, 6-card grid, list/search/clear/close. Do not restyle page 16/17.
```

Optional (non-blocking): photo encode / badge art at 400px; dark appearance not on this instance; iOS Search route placeholder is out of scope — this verdict is the Figma instance vs owner still only.
