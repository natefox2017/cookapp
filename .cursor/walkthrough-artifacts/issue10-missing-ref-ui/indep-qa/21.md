# Independent UI QA — Page 21 Add Recipe Menu

- Instance: `119:13222` (file `FHbikS2jILAeMv8mote0vD`)
- Capture: ONE `get_screenshot` after 88s stagger, `maxDimension` 400 (185×400 PNG; original 440×956)
- Still pack: **no dedicated still**. `docs/ui-screenshots/missing-ref/16-21/IMG_4446.PNG` is Search scoped browse (Recently Added / Main / Stew / Baking) — **not** page 21. Do not use 4446 as 1:1 ref.
- Expected: Cookbook + Add Recipe popover (Browse Web / From Scratch / etc.)
- iOS: `AddRecipeMenuShellView` remains `RoutePlaceholderView` (`CookbookShellViews.swift`) — no live app chrome to compare.

```
VERDICT: FAIL
Screenshot fidelity: FAIL — live `119:13222` is PixelBase only (Recipe Detail: Beef Bourguignon hero, servings 4½, ingredients list, Cook / Add to Meal Plan / Add to Groceries). Not Cookbook + Add Recipe popover. Not Search browse IMG_4446 (correctly unused). Hidden expected layers (prior mapping): W02 Cookbook, Scrim, Add Recipe Popover (Browse Web / From Scratch / etc.).
Liquid Glass: FAIL — Add Recipe popover not visible; no regular-glass menu over Cookbook. Visible surface is content-layer recipe detail (PixelBase), not nav chrome.
Consistency (shared chrome/tokens): FAIL — floating tab bar + search circle, Cookbook title/grid, and shared glass popover rows are absent; PixelBase recipe chrome is a different screen family.
Control states: FAIL — popover rows (default / hover / active / disabled / loading) not present; only PixelBase recipe controls.
Violations: (1) PixelBase-only render of `119:13222` — recipe detail instead of Add Recipe Menu. (2) Cookbook instance hidden. (3) Scrim hidden. (4) Add Recipe Popover hidden (Browse Web / From Scratch / remaining rows). (5) No dedicated owner still; using IMG_4446 would be a second FAIL (Search scoped browse). (6) App shell is placeholder-only, cannot prove kit states.
Required redo: Hide PixelBase on `119:13222`. Unhide Cookbook + Scrim + Add Recipe Popover only (Browse Web / From Scratch / etc.). Do not substitute IMG_4446 or invent Search browse. Then re-QA the same instance.
```
