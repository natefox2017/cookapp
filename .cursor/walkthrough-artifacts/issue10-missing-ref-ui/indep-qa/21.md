# Independent UI QA — Page 21 Add Recipe Menu

Agent: independent (no implementer edits). Figma not modified.  
Instance: `119:13222` (file `FHbikS2jILAeMv8mote0vD`).  
Evidence: wait 50s, then **one** `get_screenshot` `119:13222` `maxDimension` 400 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/21-live.png` (185×400; original 440×956).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Pack: **no dedicated still**. `docs/ui-screenshots/missing-ref/16-21/IMG_4446.PNG` is Search scoped browse (Recently Added / Main / Stew / Baking) — **not** used as 1:1 ref.

Expected (this page): Cookbook + scrim + Add Recipe popover (Browse Web / From Scratch / etc). PixelBase recipe-detail must be hidden.

```
VERDICT: PASS
Screenshot fidelity: PASS — Live `119:13222` is Cookbook with dimmed recipe grid + Add Recipe popover (Browse Web / From Scratch / Scan or Pick / Enter URL / Paste), not PixelBase Recipe Detail and not Search browse. Green Cookbook title, circular header + / …, four photo cards remain visible through the scrim; no Beef Bourguignon hero / servings / ingredients / Cook / Meal Plan / Groceries stack. IMG_4446 correctly unused.
Liquid Glass: PASS — Regular glass on the Add Recipe popover, circular header buttons, floating tab bar, and search circle only. Recipe cards stay content-layer (not glass slabs). No nested glass-on-glass panels. Light appearance present on this frame.
Consistency (shared chrome/tokens): PASS — Green Large title, circular icon buttons, floating tab + search circle, Cookbook active green pill match shared chrome. Popover is glass menu rows with + / chevron, not a per-screen fork and not Search section chips.
Control states: PASS* — Default popover rows; Cookbook tab active. Hover / disabled / loading not shown on this static frame (shared-kit residual, not a per-screen fork).
Violations: none blocking.
Required redo: none.
```

Non-blocking: iOS `AddRecipeMenuShellView` is still `RoutePlaceholderView` (`CookbookShellViews.swift`) — this verdict is the Figma instance only. Dark appearance not on this instance.
