# Independent UI QA — Page 22 New Recipe

Agent: independent (no implementer edits). Figma not modified.  
Instance: `119:13349` (file `FHbikS2jILAeMv8mote0vD`). Source `118:11949`.  
Owner stitch: `docs/ui-screenshots/missing-ref/22/IMG_4448.PNG` + `IMG_4449.PNG`.  
Not this instance: `IMG_4450` (time picker), `IMG_4451` (Add Photo menu), `IMG_4452–4455` (Category/Cuisine lists).  
Evidence: stagger 96s, **one** `get_screenshot` `119:13349` `maxDimension` 500 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/22-figma-119-13349-md500.png` (184×500; original 440×1200).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Checked: empty New Recipe — Name/Description, Add Time, Cover, Ingredients/Steps CTAs, Categories, Cuisine, Manage, Delete.  
iOS `NewRecipeShellView` is a route placeholder; this verdict is the Figma instance only.

```
VERDICT: FAIL
Screenshot fidelity: FAIL — Live `119:13349` is empty New Recipe (not PixelBase): X + green ✓; Large green “New Recipe”; Name/Description card; Total Time + Add Time >; Add Cover Photo +; Ingredients header + split Add Recipe | New Ingredient; Add New Ingredient Section; Steps with red minus + New Step; Add New Step Section; Categories >; Cuisine >; Manage Fields; Delete Recipe. Stitch covers `IMG_4448` + `IMG_4449` content (compact `4449` nav title is scroll chrome, not required on this tall Large-title frame). 1:1 still fails: Cover leading glyph is a hollow rectangle, not the owner landscape-photo (mountains) icon on `IMG_4448`.
Liquid Glass: PASS — Glass only on nav/control layer (circular X, confirm ✓). Form cards, section CTAs, Categories/Cuisine/Manage/Delete are opaque content, not glass slabs. Light appearance present on this frame. Dark not on this instance (non-blocking vs other empty-form stills).
Consistency (shared chrome/tokens): PASS — Circular X/✓, green Large title, white grouped cards, green + / CTAs, red minus + Delete Recipe match shared editor kit (page 13 empty). No per-screen tab fork (no tab bar, correct for compose sheet).
Control states: PASS* — Default X / ✓ / + / minus / Add Time / split CTAs / Manage / Delete evidenced. Hover / active / disabled / loading not shown on this static still (shared-kit residual, not a per-screen fork).
Violations:
1. Cover photo leading icon on `119:13349` / source `118:11949` is a generic rectangle vs Pestle landscape-photo glyph on `IMG_4448` — replace with matching SVG (same optical size as Time/Ingredients icons); do not leave a hollow frame.
Required redo: Fix Cover icon on source `118:11949` (instance `119:13349` inherits); keep PixelBase hidden; re-export empty stitch vs `IMG_4448` + `IMG_4449` only (do not bake `4450`/`4451`/`4452–4455` into this node).
```
