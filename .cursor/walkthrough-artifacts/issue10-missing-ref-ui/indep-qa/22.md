# Independent UI QA — Page 22 New Recipe (empty)

Agent: independent (no implementer edits). Figma not modified.  
Instance: `119:13349` ← source `118:11949` (file `FHbikS2jILAeMv8mote0vD`).  
Evidence: wait 125s, **one** `get_screenshot` `119:13349` `maxDimension` 1200 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/22-live.png` (440×1200; original 440×1200).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Pack: owner stitch `docs/ui-screenshots/missing-ref/22/IMG_4448.PNG`–`IMG_4455.PNG`.

Scope: **empty New Recipe**, not filled editor 13. Default unrolled form vs `IMG_4448` + lower cards in `IMG_4449`. Compact centered title on `4449` is a scroll/collapse state, not missing chrome on this unrolled frame. `IMG_4450` (time wheels) and `IMG_4451` (Add Photo popover) are overlays of this screen — not required on the default instance. `IMG_4452`–`IMG_4455` are Category / Cuisine pickers — not this instance.

```
VERDICT: FAIL
Screenshot fidelity: FAIL — Live `119:13349` is empty New Recipe (not filled editor 13): X + check, green title “New Recipe”, empty Name/Description, Total Time + Add Time >, Add Cover Photo, Ingredients + Add Recipe | New Ingredient, Add New Ingredient Section, Steps (red minus + New Step), Add New Step Section, Categories, Cuisine, Manage Fields, red Delete Recipe. Structure/order vs concatenated `IMG_4448`+`IMG_4449` is correct. FAIL vs `IMG_4448`: Cover Photo leading glyph is an empty rounded-rect/checkbox, not the filled photo/landscape icon. FAIL vs `IMG_4449`: Cuisine leading glyph is a plain orange ring, not fork+knife. Confirm circle is opaque solid green vs stitch glass-tint check (`4448`). Island/status/keyboard ignored.
Liquid Glass: FAIL — Close X reads as regular glass on the nav layer; content cards stay opaque (correct). Confirm (✓) is a solid brand-green disc, not regular glass with sparse green tint as in `IMG_4448` / shared circular confirm. Light appearance present; dark not on this instance.
Consistency (shared chrome/tokens): FAIL — Circular confirm diverges from stitch glass check used on this sheet and on Category/Cuisine (`4452`/`4454`). Cover Photo and Cuisine icons fork from Pestle row glyphs. Green Large title, X, red minus, green section CTAs, destructive Delete otherwise match the empty-editor kit. Do not fork filled-13 fields onto this screen.
Control states: PASS* — Default empty: time collapsed (`>` not wheels), no photo menu, no Category/Cuisine sheets. Hover / disabled / loading not on this static frame (shared-kit residual).
Violations:
  1. On source `118:11949` / instance `119:13349`, replace Add Cover Photo leading icon with the photo/landscape glyph from `IMG_4448` (not empty rounded-rect).
  2. Replace Cuisine leading icon with orange fork+knife (utensils) from `IMG_4449` (not a plain ring).
  3. Restyle header confirm (✓) to regular Liquid Glass (translucent circle, green-tint check) matching `IMG_4448`; do not use an opaque solid green fill.
Required redo: Patch source `118:11949` (instance follows) for (1)–(3) only; keep empty Name/Description and no filled-13 ingredients/steps/nutrition. Re-export tall `119:13349` vs concatenated `IMG_4448`–`IMG_4449` for the default form (ignore island/status/keyboard). Do not demand `4450`–`4451` overlays or `4452`–`4455` pickers on this node. Do not steal page 13 filled editor content or edit Bottom NAV.
```
