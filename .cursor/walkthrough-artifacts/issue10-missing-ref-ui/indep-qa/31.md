# Independent UI QA — Page 31 Add Menu

Agent: independent (no implementer edits). Figma not modified. Bottom NAV `77:237` not edited.  
Instance: `119:12435` (file `FHbikS2jILAeMv8mote0vD`).  
Owner: `docs/ui-screenshots/missing-ref/29-32/IMG_4467.PNG`.  
Evidence: wait 36s, then **one** `get_screenshot` `119:12435` `maxDimension` 400 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/31-live.png` (185×400; original 440×956).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.

Must-check: empty days + add menu; peek is photo + **4½** without Beef Bourguignon title.

```
VERDICT: FAIL
Screenshot fidelity: FAIL vs `29-32/IMG_4467.PNG`. Empty-week body is present (Sun 13–Yesterday “No recipes”, green **Today**, cart, Meal Plan tab, four-row add menu) — not a filled week and not PixelBase. Peek under the tab bar is photo + **4½** with **no** Beef Bourguignon title (prior peek-title FAIL cleared). Fail 1:1 on menu glyphs vs IMG_4467: Add Recipe is **+**, Add Note is **+**, Add Random Recipe is a **book**; owner is recipe-card / note / sparkle (Add Section clock + › matches). Island/status/keyboard ignored.
Liquid Glass: PASS* — Floating tab + search over the stew peek read as nav-layer glass. Add menu shows a right-edge green bloom (not a solid green control disc as the only chrome). Brand green tints Today / day + / Meal Plan selected, not the whole glass surface. Light only on this frame. Residual: menu card still reads mostly opaque over empty days (owner is regular glass + bloom); not enough alone to fail given white empty-day backdrop.
Consistency (shared chrome/tokens): FAIL — green Meal Plan title, cart circle, day +, floating tab + search, Meal Plan selected match shared chrome. Shared **popover** is still forked: + / clock / + / book stand-ins vs Pestle recipe-card / section-clock / note / sparkle on IMG_4467. Peek card now uses the owner servings-pill pattern (photo + 4½, no title) — that fork is cleared.
Control states: PASS — add menu open; **Add Section ›** collapsed chevron; Meal Plan tab selected; cart / + default. Do not fail solely for a missing hover/disabled/loading matrix on this pack.
Violations:
1. On source of instance `119:12435`, restore Pestle add-menu glyphs to match IMG_4467: Add Recipe = recipe card (not +), Add Note = note (not +), Add Random Recipe = sparkle (not book). Keep Add Section clock + ›. Keep photo + 4½ peek with no Beef Bourguignon title.
Required redo: Patch instance `119:12435` (and its source) for (1) only. Keep empty “No recipes” days + green Today + cart + Meal Plan tab + glass tab/search + green bloom on the menu. Keep PixelBase hidden. Do not edit Bottom NAV `77:237`. Re-export `119:12435` and re-QA.
```
