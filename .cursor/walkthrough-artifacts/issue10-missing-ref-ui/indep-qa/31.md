# Independent UI QA — Page 31 Add Menu

Agent: independent (no implementer edits). Figma not modified. Bottom NAV `77:237` not edited.  
Instance: `119:12435` (file `FHbikS2jILAeMv8mote0vD`).  
Owner: `docs/ui-screenshots/missing-ref/29-32/IMG_4467.PNG`.  
Evidence: wait 70s, then **one** `get_screenshot` `119:12435` `maxDimension` 400 → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/31-live.png` (185×400; original 440×956).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.

```
VERDICT: FAIL
Screenshot fidelity: FAIL vs `29-32/IMG_4467.PNG`. Empty-week body is present (Sun 13–Yesterday “No recipes”, green **Today**, cart, Meal Plan tab, four-row add menu) — not a filled week and not PixelBase. Fail 1:1: (1) beef peek is a titled **Beef Bourguignon (Beef Burgundy)** card; owner is photo-only peek with **4½** people servings pill and no title. (2) Menu glyphs are + / clock / + / book stand-ins, not Pestle recipe-card / section-clock / note / sparkle on IMG_4467. (3) Owner menu sits as a floating glass card with right-edge green bloom over Mon/Tue; live menu is a tighter opaque panel without that bloom.
Liquid Glass: FAIL — tab/search over the stew peek reads as nav-layer glass (good). Add menu is an opaque white slab (content does not read through; no hairline + green bloom). Brand green tints Today / + / Meal Plan, not a flooded glass fill. Light only on this frame.
Consistency (shared chrome/tokens): FAIL — green Meal Plan title, cart circle, day +, floating tab + search, Meal Plan selected match shared chrome. Shared **popover** is forked: wrong row icons vs owner Pestle menu; peek card uses a titled recipe-card pattern instead of the owner servings-pill peek used on this still.
Control states: PASS* — add menu open; **Add Section ›** collapsed chevron; Meal Plan tab selected; cart / + default. Hover / disabled / loading not on this static Pestle still (kit residual, not a 31-only miss).
Violations:
1. Peek under the tab bar on `119:12435`: photo + **4½** servings pill only (IMG_4467); hide the Beef Bourguignon title overlay.
2. Restore Pestle add-menu glyphs: Add Recipe (recipe card), Add Section (clock+ ›), Add Note (note), Add Random Recipe (sparkle) — not +/clock/+/book.
3. Menu material: regular translucent glass, low-contrast hairline, right-edge green bloom over empty days — not an opaque white card.
Required redo: Patch instance `119:12435` (and its source) for (1)–(3). Keep empty “No recipes” days + green Today + cart + Meal Plan tab. Keep PixelBase hidden. Do not edit Bottom NAV `77:237`. Re-export `119:12435` and re-QA.
```
