# Independent UI QA — Page 32 Add Section nested

Instance: `119:12521` (file `FHbikS2jILAeMv8mote0vD`)
Evidence: ONE `get_screenshot` node `119:12521` `maxDimension` 400 (185×400). Status bar / Dynamic Island / keyboard ignored.
Owner nested target: Breakfast / Lunch / Dinner + Manage Times (not page 31 four-item menu). Owner still `docs/ui-screenshots/missing-ref/29-32/IMG_4467.PNG` is the **31** four-item menu; nested state is recording-led.

```
VERDICT: FAIL
Screenshot fidelity: FAIL — live instance is not a clean nested Add Section over empty Meal Plan. Nested labels (Breakfast / Lunch / Dinner / Manage Times) appear, but they sit on a filled-recipe raster (Beef Bourguignon, Mayonnaise, servings pills) with duplicate date copy (Sunday, Sep 13 vs Tuesday, Sep 14 / “No recipes”) bleeding through. That is PixelBase + reconstructed chrome, not 1:1 vs the nested recording. Parent “+ Add Recipe” / “Add Section” also remain visible with the nested list (stacked menus).
Liquid Glass: FAIL — cart, overlay, and tab/search exist as chrome, but the body is a photo slab (PixelBase) with glass/menu frames stacked on the raster. Content-layer glass-on-photo and stacked overlays violate nav-only Liquid Glass (no glass-on-glass / no PixelBase as the page).
Consistency (shared chrome/tokens): FAIL — Meal Plan title, cart circle, + pills, and tab bar are present, but the screen is not a kit composition: PixelBase photograph supplies recipe cards/scrims while a second reconstructed date list and menus overlay it. Shared popover is not a single glass menu.
Control states: PARTIAL — Meal Plan tab selected; Add Section reads expanded (chevron). Nested rows Breakfast / Lunch / Dinner / Manage Times are the correct *set* vs page 31 (Add Recipe / Add Section › / Add Note / Add Random Recipe). Unusable because the expanded menu is duplicated over PixelBase and parent items.
Violations: (1) PixelBase (or equivalent filled raster) is visible on instance `119:12521` — FAIL per brief. Hide PixelBase on source + this instance; never `resetOverrides()`. (2) Duplicate stacked menus: parent Add Recipe / Add Section remain while nested Breakfast–Manage Times is open; ship one nested panel per recording, not 31 menu + 32 menu stacked. (3) Empty-day list (“No recipes”) and filled cards (Beef / Mayo) occupy the same artboard — unhide only the nested-state chrome (empty days + Today peek + nested menu); keep filled 03 cards off this instance. (4) Overlapping date headers (Sunday vs Tuesday) from two layer stacks — one date column only.
Required redo: On claimed source `118:11143` and instance `119:12521`, set every `PixelBase` `visible: false`. Unhide a single nested Add Section frame (Breakfast, Lunch, Dinner, Manage Times) and Meal Plan empty-day chrome; hide the page-31 four-item menu and any second menu instance. Do not leave reconstructed layers and the raster both visible. Re-export `119:12521` and re-QA until PixelBase and stacked menus are gone.
```
