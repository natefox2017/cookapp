# Independent UI QA — Page 38 Experiments

Agent: independent (no implementer edits). Figma not modified.  
Instance: `117:10209` (file `FHbikS2jILAeMv8mote0vD`). Source `116:18972`.  
Owner still: `docs/ui-screenshots/missing-ref/38-40/IMG_4486.PNG` (**not** `IMG_4487` Account).  
Evidence: one `get_screenshot` `117:10209` `maxDimension` 400 after 144s stagger → `.cursor/walkthrough-artifacts/issue10-missing-ref-ui/indep-qa/38-figma-117-10209.png` (185×400; original 440×956).  
Rules: `ui-screenshots.mdc` (ignore status bar / Dynamic Island / keyboard), `ui-fidelity.mdc`, `ui-consistency-lock.mdc`, `liquid-glass.mdc`.  
Checked: circular Back, green **Experiments** title, **Faster Recipe Loading** OFF + hint, **Use Experimental Parser** OFF + hint.

```
VERDICT: PASS
Screenshot fidelity: PASS — Live `117:10209` is Experiments (not PixelBase, not Account/`IMG_4487`). Circular Back chevron; large green Experiments title; two white pill rows with iOS-style switches **both OFF**; hints “Disable this if any of your recipes are missing. Requires a restart to take effect.” and “Recipes which cannot be parsed normally will use an experimental parser to try and extract some recipe information. These recipes may be garbage.” Empty rest of sheet. Matches owner `IMG_4486.PNG` after ignoring status bar / island / keyboard.
Liquid Glass: PASS — Glass only on nav/control layer (circular Back). Toggle rows and hints are opaque content cards, not glass slabs. Light appearance present on this frame.
Consistency (shared chrome/tokens): PASS — Circular Back, large green title, white Settings-style toggle cards reuse shared chrome. Screen was not restyled into page 39 Account.
Control states: PASS* — Default OFF evidenced for both toggles; Back default evidenced. Hover / active / disabled / loading and ON not shown on this static still (shared-kit residual, not a per-screen fork). Owner still is OFF-only.
Violations: none.
Required redo: none.
```

Optional (non-blocking): toggle-track illustration and sheet outer radius slightly simplified at 400px maxDimension vs the photo crop; dark appearance not on this instance; iOS `ExperimentsShellView` remains a route placeholder — this verdict is the Figma instance only.
