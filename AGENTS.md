# cookapp

食谱应用。UI 约束见 [DESIGN.md](DESIGN.md)。

生成或修改界面前：

1. 读 `DESIGN.md`
2. 加载 `.cursor/skills/` 里对应的 Design Skill
3. 产品组件必须包含 default / hover / active / disabled / loading；严格录屏/截图复刻仅实现来源可见或需求明确的状态，其他状态标记为未知
4. 如为录屏/截图复刻，先读 `.cursor/rules/reference-fidelity.mdc`；只使用授权来源，并完成原始 viewport 的逐图叠加/导线检查后才可交付
