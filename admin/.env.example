# Local/dev only — mock Admin API payloads (default true in Vite DEV)
VITE_ADMIN_USE_MOCK=true

# Cloud Admin API base URL (required for live mode) — project origin, not /functions/v1
# VITE_ADMIN_API_BASE_URL=https://semsjyrqjnumpvanibip.supabase.co

# Production builds MUST leave VITE_ADMIN_USE_MOCK unset or false (Issue #51).
